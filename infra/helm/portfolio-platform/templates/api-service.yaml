{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio-platform.fullname" . }}-api
  labels:
    {{- include "portfolio-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "portfolio-platform.api.selectorLabels" . | nindent 4 }}
{{- end }}
