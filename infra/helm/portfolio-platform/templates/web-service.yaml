{{- if .Values.web.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio-platform.fullname" . }}-web
  labels:
    {{- include "portfolio-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "portfolio-platform.web.selectorLabels" . | nindent 4 }}
{{- end }}
